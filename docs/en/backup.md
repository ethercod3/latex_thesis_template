# Backup

This section is included when `include_backup_tools` is enabled.

The backup helper creates a `git bundle` and can upload it to rclone destinations:

```bash
task backup:local
task backup:dry
task backup
```
