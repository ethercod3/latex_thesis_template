# Diagrams

This section is included when `include_diagram_tools` is enabled.

Mermaid and Python diagrams live in separate directories and are generated with:

```bash
task mermaid
task diagrams
```
